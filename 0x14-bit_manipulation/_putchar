#include <unistd.h>
/**
  * _putchar – It writes the c char in the standard header
  * @c- The character to be printed
  *
  * Return- if successful it returns 1
  * On error -1 is returned and the errno is set appropriately.
 */

int _putchar(char c)
{

	   return (write(1, &c, 1));
}

